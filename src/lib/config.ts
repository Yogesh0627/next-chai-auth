export const  mailOptions = {
    host: "sandbox.smtp.mailtrap.io",
    port: 2525,
    auth: {
      user: "6556548fea39a2", // ❌ doesn't need to be here confidentail should be in env host, port too
      pass: "31151778829aac" // ❌ doesn't need to be here confidentail should be in env host, port too
    }
  }